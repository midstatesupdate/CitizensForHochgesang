import {RouteLoading} from '@/components/route-loading'

export default function Loading() {
  return (
    <RouteLoading
      label="News"
      title="Loading campaign updates"
      description="Gathering the latest campaign announcements and articles."
    />
  )
}
