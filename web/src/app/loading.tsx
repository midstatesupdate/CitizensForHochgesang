import {RouteLoading} from '@/components/route-loading'

export default function Loading() {
  return (
    <RouteLoading
      label="Loading"
      title="Preparing campaign content"
      description="Fetching the latest campaign updates, events, and media."
    />
  )
}
